{
  "name": "owlbearosds",
  "description": "This is approximately the simplest possible Meet add-on.",
  "keywords": [
    "Google Meet"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p ../dist/owlbearosds && cp src/*.html && cp src/*.png ../dist/owlbearosds/ && npx webpack"
  },
  "dependencies": {
    "@googleworkspace/meet-addons": "^1.2.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  }
}
